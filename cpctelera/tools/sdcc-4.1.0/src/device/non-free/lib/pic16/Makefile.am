## Makefile.am -- Process this file with automake to produce Makefile.in

SUBDIRS = libdev

include $(top_srcdir)/Makefile.common

DISTCLEANFILES = a.cod a.hex a.lst pics.supported

distclean-local:
	rm -rf ./.checkdevices ./autom4te.cache
