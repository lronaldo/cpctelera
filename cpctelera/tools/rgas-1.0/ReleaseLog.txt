v1.0
* Allow select/copy/paste pixel blocks (uses system clipboard so works between files).
* Colour-picker tool
* Recent file list
* Generate multiple incrementally rotated sprites from base
* Ctrl+S shortcut to save

v0.100
* Include sample mode zero chars
* CPC+ sprites to have configurable aspect ratio in both horiz/vert of 1, 2, or 4x
* Zoom in/out with Ctrl +/-
* Import sprite from binary
* Recolour sprite (col1 -> col2)

v0.99
* Pen/brush size tool
* Allow drawing filled rectangle on drawing surface.
* When importing bitmap, allow import from clipboard or file
